server:
  port: 8082
spring:
  application:
    name: "movie_feedback"
  datasource:
    url: jdbc:h2:./db/test;AUTO_SERVER=TRUE
    username: root
    password: eldo
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always


